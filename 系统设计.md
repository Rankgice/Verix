# Verix 系统设计

本文档说明当前 Verix 项目的目录结构、业务流转方式和插件扩展实现。当前插件机制是**新增能力**，不会替换或迁移原有数据库、TestSpec、HTTP/gRPC 测试等功能。

---

## 一、项目目录和文件

> 下面列出项目中需要开发者关注的源码、配置、设计文档和测试文件。`.git/`、`.idea/`、`.claude/worktrees/` 等版本控制和 IDE/临时目录不属于程序运行目录，因此不展开列出。

```text
Verix/
├── main.go                         # 程序入口，创建 MCP Server 并通过标准输入输出启动服务。
├── go.mod                          # Go 模块名称、Go 版本和直接依赖声明。
├── go.sum                          # Go 依赖的校验和。
├── .gitignore                      # Git 忽略规则。
├── test.json                       # 本地示例 TestSpec 测试文件。
├── 需求.md                         # 数据库 MCP 能力和安全规则的需求说明。
├── 设计方案.md                     # TestSpec v1 的输入、断言、报告和执行设计。
├── AGENTS.md                       # 项目目录说明、开发约束和常用验证命令。
│
├── core/
│   └── core.go                     # 组装现有 MCP Tools、Resources，并接入插件 MCP Tools。
│
├── tools/                          # 现有 MCP Tool 适配层，本次没有迁移为插件。
│   ├── common.go                   # 读取和解析 TestSpec 输入。
│   ├── db_common.go                # 数据库 Tool 共用的连接管理器。
│   ├── initialize_db.go            # 初始化运行时数据库连接的 MCP Tool。
│   ├── execute_sql.go              # 执行 SQL 的 MCP Tool。
│   ├── list_tables.go              # 列出数据库表的 MCP Tool。
│   ├── get_schema.go               # 获取数据库概要结构的 MCP Tool。
│   ├── describe_table.go           # 获取单表详细结构的 MCP Tool。
│   ├── analyze_query.go             # 分析 SQL 风险但不执行的 MCP Tool。
│   ├── validate_spec.go             # 校验 TestSpec 的 MCP Tool。
│   └── run_spec.go                  # 执行 TestSpec 的 MCP Tool。
│
├── db/                             # 现有数据库业务实现，本次保持原样。
│   ├── types.go                    # 数据库接口、连接配置、查询结果和 Schema 类型。
│   ├── config.go                   # 数据库连接配置解析和规范化。
│   ├── manager.go                  # 多数据库连接管理和运行时连接管理。
│   ├── mysql.go                    # MySQL database/sql 执行和 Schema 查询实现。
│   ├── sql_analysis.go             # SQL 操作分析、安全校验和 LIMIT 改写。
│   ├── config_test.go              # 数据库配置解析测试。
│   ├── manager_runtime_test.go     # 数据库运行时连接状态测试。
│   └── sql_analysis_test.go        # SQL 分析和安全规则测试。
│
├── engine/                         # 现有 TestSpec 领域核心，本次保持原样。
│   ├── types.go                    # TestSpec、请求、断言、报告和 Diff 类型。
│   ├── runner.go                   # TestSpec 校验、HTTP/gRPC 执行、断言和诊断。
│   └── AGENTS.md                   # engine 包的行为约束和修改注意事项。
│
├── resources/
│   └── examples.go                 # 注册内置 TestSpec 示例 Resource。
│
├── sdk/                            # 插件开发使用的稳定 SDK。
│   ├── protocol/
│   │   └── protocol.go             # 插件协议 DTO、Manifest、生命周期消息、Host API 消息和错误码。
│   ├── rpc/
│   │   ├── rpc.go                  # 双向 JSON-RPC 风格 stdio Peer、消息 framing、并发请求和取消。
│   │   └── rpc_test.go              # RPC 双向调用、消息 framing 和取消测试。
│   └── plugin/
│       └── plugin.go               # 插件运行时 SDK，自动注册生命周期方法并提供 HostClient。
│
├── pluginhost/                     # 主程序中的插件宿主实现。
│   ├── host.go                     # 插件发现、Manifest 校验、EXE 启动、握手、调用、权限和 Host API。
│   ├── mcp.go                      # 将插件能力注册为 plugin_list、plugin_describe、plugin_call 三个 MCP Tool。
│   └── host_test.go                # Manifest 校验和插件发现测试。
│
├── cmd/
│   ├── example-plugin/
│   │   └── main.go                 # 示例独立插件，提供 echo 方法并调用 host.log 验证双向通信。
│   └── dao-plugin/
│       ├── main.go                 # DAO 生成插件入口，注册 generate 和 list_tables 方法。
│       ├── generator.go            # 数据库表结构读取、MySQL→Go 类型映射、命名转换和模板数据组装。
│       ├── template.go             # 嵌入 model.go.tmpl 并用 go/format 渲染生成 GORM model 代码。
│       ├── model.go.tmpl           # 符合 Go GORM Model Standards 的 GORM model 代码模板。
│       └── generator_test.go       # 类型映射、命名转换和模板渲染测试。
│
└── plugins/
    ├── example/
    │   └── plugin.json             # 示例插件的静态 Manifest，声明启动方式、权限和业务方法。
    └── dao-plugin/
        └── plugin.json             # DAO 生成插件的 Manifest，声明 generate/list_tables 方法和输入输出 Schema。
```

### 目录之间的依赖关系

```text
main.go
  └── core.NewServer()
        ├── tools/       ──> db/、engine/
        ├── resources/
        └── pluginhost/
                ├── sdk/protocol/
                ├── sdk/rpc/
                └── 启动外部插件 EXE

插件 EXE
  └── sdk/plugin/
        ├── sdk/protocol/
        └── sdk/rpc/
```

重要边界：

- `db/` 和 `engine/` 是当前主程序的业务代码，不是插件代码。
- `pluginhost/` 只负责插件生命周期、路由和 Host API，不理解插件业务。
- `sdk/protocol/` 是跨进程契约，不能依赖 `db/`、`engine/` 或 MCP SDK。
- `sdk/plugin/` 是插件作者使用的高级封装。
- `plugins/example/plugin.json` 只描述插件，不包含插件实现逻辑。

---

## 二、整体业务流转路线

### 2.1 主程序启动流程

```text
操作系统启动 verix
        │
        ▼
main.go
        │
        ▼
core.NewServer()
        │
        ├── 注册现有 TestSpec Tools
        ├── 注册现有 Database Tools
        ├── 注册现有 Resource
        │
        └── 创建 pluginhost.Manager
                │
                ├── 扫描插件目录
                │       优先 VERIX_PLUGIN_DIR
                │       否则 verix.exe 所在目录下的 plugins/
                │
                ├── 读取每个 plugin.json
                │
                ├── 校验 Manifest
                │
                └── 注册三个插件 MCP Tools
                        ├── plugin_list
                        ├── plugin_describe
                        └── plugin_call
        │
        ▼
server.Run(StdioTransport)
        │
        ▼
等待 MCP 客户端请求
```

插件默认是懒启动的。主程序发现插件时只读取 Manifest，不会立即启动所有插件 EXE。

### 2.2 MCP 客户端查看插件

```text
MCP 客户端调用 plugin_list
        │
        ▼
主程序 pluginhost.Manager.List
        │
        ▼
返回插件 ID、名称、版本、状态和方法数量
```

`plugin_list` 不启动插件，也不返回完整方法 Schema。

### 2.3 MCP 客户端查看插件详情

```text
MCP 客户端调用 plugin_describe
        │
        ▼
Manager 根据插件 ID 或名称查找 Manifest
        │
        ▼
如果插件未启动，则启动插件 EXE
        │
        ▼
创建 stdin/stdout 管道
        │
        ▼
启动 sdk/rpc.Peer 的读循环
        │
        ▼
发送 plugin.initialize
        │
        ▼
插件 sdk/plugin 返回初始化结果
        │
        ▼
主程序发送 plugin.describe
        │
        ▼
插件返回业务方法、输入 Schema 和输出 Schema
        │
        ▼
返回给 MCP 客户端
```

### 2.4 MCP 客户端调用插件

```text
MCP 客户端调用 plugin_call
        │
        ▼
主程序校验 plugin 和 method
        │
        ▼
确保插件已经 ready
        │
        ▼
获取插件并发信号量
        │
        ▼
生成 call_id 和 deadline
        │
        ▼
发送 plugin.invoke
        │
        ▼
插件 sdk/plugin 找到业务 Handler
        │
        ▼
Handler 解码 arguments
        │
        ▼
执行业务逻辑
        │
        ├── 不需要主程序能力
        │       └── 直接生成结果
        │
        └── 需要主程序能力
                └── 通过 HostClient 发起 host.* RPC
        │
        ▼
插件返回 InvokeResult
        │
        ▼
主程序返回 plugin_call 结果
        │
        ▼
MCP 客户端获得业务结果
```

### 2.5 插件反向调用主程序 Host API

```text
插件 Handler
    │
    ▼
call.Host.Log / ConfigGet / StorageGet / StoragePut
    │
    ▼
HostClient 调用 sdk/rpc.Peer.Call
    │
    ▼
发送 host.log 或 host.storage.* 请求
    │
    ▼
主程序 Peer 收到请求
    │
    ▼
根据方法找到 Host Handler
    │
    ├── 检查插件 Manifest 权限
    ├── 解析请求参数
    ├── 执行主程序 Host 服务
    └── 返回结果
    │
    ▼
插件 HostClient 收到响应
    │
    ▼
原业务 Handler 继续执行
```

双向 RPC 的关键点是：主程序等待插件结果时，仍然持续读取插件发来的 Host 请求，因此不会因为互相等待而死锁。

### 2.6 主程序退出流程

```text
主程序收到退出信号
        │
        ▼
Manager.Shutdown
        │
        ▼
向已启动插件发送 plugin.shutdown
        │
        ▼
插件停止接收新业务请求并返回
        │
        ▼
主程序关闭或终止插件进程
        │
        ▼
MCP Server 退出
```

---

## 三、编码层面的设计

### 3.1 协议设计

插件协议使用 JSON-RPC 2.0 的请求/响应模型，传输在插件进程的 stdin/stdout 上进行。

消息格式：

```text
Content-Length: <字节数>\r\n
\r\n
<JSON 数据>
```

stdout 只能输出协议消息；插件普通日志必须写 stderr，或者使用 `host.log` 发送结构化日志。

协议分为三组：

```text
生命周期：plugin.initialize / plugin.describe / plugin.ping / plugin.shutdown
调用控制：plugin.invoke / plugin.cancel
宿主回调：host.log / host.config.get / host.storage.*
```

业务方法不直接成为顶层 RPC 方法，而是统一通过：

```json
{
  "method": "plugin.invoke",
  "params": {
    "call_id": "call-1",
    "method": "业务方法名",
    "arguments": {}
  }
}
```

这样协议控制方法和插件业务方法不会混在一起。

### 3.2 `sdk/protocol` 的职责

`protocol.go` 只定义跨进程传输的数据结构：

- `Request`、`Response`、`Error`：RPC 消息。
- `Manifest`、`Runtime`、`Permission`：插件静态声明。
- `InitializeParams`、`InitializeResult`：握手消息。
- `DescribeResult`、`Method`：插件能力描述。
- `InvokeParams`、`InvokeResult`：业务调用消息。
- `HostLogParams`、`StorageParams` 等：Host API 消息。

业务参数使用 `json.RawMessage` 或 `any`，使协议包不依赖具体业务包。

### 3.3 `sdk/rpc` 的职责

`Peer` 是一条双向 RPC 连接，核心字段含义如下：

```text
reader       读取对端消息
writer       写入本端消息
writeMu      防止多个协程交叉写消息
pending      保存等待响应的请求
handlers     保存收到请求时的处理函数
nextID       生成请求 ID
closed       标识连接已经关闭
```

调用过程：

```text
Peer.Call
  ├── 生成请求 ID
  ├── 将等待通道放入 pending
  ├── 写出请求
  ├── 等待读循环匹配响应
  └── 解析 result 或返回 error
```

读循环过程：

```text
Peer.Serve
  ├── 读取一个 framed message
  ├── 有 ID 且没有 method：当作 response
  ├── 有 method：查找 Handler 并异步执行
  └── Handler 执行结束后写回 response
```

### 3.4 `sdk/plugin` 的职责

插件作者只需要提供：

```go
plugin.Options{
    ID:      "插件唯一 ID",
    Name:    "插件名称",
    Version: "插件版本",
    Methods: map[string]plugin.Handler{...},
}
```

`plugin.Run` 自动注册：

```text
plugin.initialize
plugin.describe
plugin.ping
plugin.shutdown
plugin.cancel
plugin.invoke
```

业务 Handler 通过：

```go
call.DecodeInput(&input)
```

解析输入，通过：

```go
call.Host.Log(...)
call.Host.ConfigGet(...)
call.Host.StorageGet(...)
call.Host.StoragePut(...)
call.Host.StorageDelete(...)
```

访问主程序能力。

### 3.5 `pluginhost.Manager` 的职责

Manager 是主程序中的插件运行时，负责：

1. 发现插件目录。
2. 读取并校验 `plugin.json`。
3. 根据 Manifest 启动插件 EXE。
4. 建立 stdin/stdout RPC Peer。
5. 执行初始化握手。
6. 缓存插件 Describe 结果。
7. 路由业务调用。
8. 控制并发数量。
9. 设置调用超时。
10. 检查 Host API 权限。
11. 管理插件状态。
12. 关闭插件进程。

Manager 不负责理解 `echo`、`get_weather`、`create_issue` 等业务方法。

### 3.6 Manifest 设计

Manifest 是插件的静态声明，主要字段如下：

```text
manifest_version       Manifest 格式版本
protocol_version       RPC 协议版本
id                     全局唯一插件 ID
name                   插件名称
version                插件版本
runtime.command        插件 EXE 相对路径
runtime.*              启动、超时、并发和重启配置
permissions            插件可使用的 Host API
methods                插件业务方法和 JSON Schema
```

命令路径必须是插件目录内的相对路径，防止 Manifest 指向任意外部程序。

### 3.7 Host API 设计原则

插件 SDK 不直接暴露主程序内部对象：

```text
不暴露 *sql.DB
不暴露 db.Manager
不暴露 engine.TestSpec 的内部执行状态
不暴露 mcp.Server
```

而是暴露稳定的 DTO 和方法：

```text
host.log
host.config.get
host.storage.get
host.storage.put
host.storage.delete
```

这样主程序内部以后可以重构，插件只依赖稳定的跨进程契约。

### 3.8 权限设计

插件在 Manifest 中声明权限，例如：

```json
{
  "permissions": [
    {"name": "host.log"},
    {"name": "host.storage"}
  ]
}
```

主程序收到插件请求时检查：

```text
插件身份
  ↓
Manifest 权限
  ↓
请求的 Host 方法
  ↓
是否允许
  ↓
执行或返回 PermissionDenied
```

### 3.9 现有能力与插件 SDK 的关系

当前版本不把 `db/`、`engine/`、`tools/` 改成插件。它们继续由主程序直接调用：

```text
现有 MCP 请求
  └── tools/ ──> db/ 或 engine/
```

插件只能调用已经明确开放的 Host API。未来如果确实需要把原有能力开放给插件，应新增稳定 Facade，例如：

```text
host.verix.run_spec
host.verix.analyze_sql
```

而不是让插件直接 import 主程序内部包。

### 3.10 当前版本的范围

已经实现：

- 插件发现。
- Manifest 校验。
- 独立 EXE 启动。
- 初始化、描述、调用、取消、关闭协议。
- 双向 RPC。
- `plugin_list`、`plugin_describe`、`plugin_call`。
- `host.log`、配置读取和内存 Storage。
- 示例插件和示例 Manifest。
- DAO 生成插件（`com.verix.dao`），根据 MySQL 表结构生成符合 Go GORM Model Standards 的 GORM model 代码。

暂未实现：

- 在线安装和升级。
- 插件签名校验。
- 操作系统级沙箱。
- Host HTTP 服务。
- 动态生成每个业务方法的 MCP Tool。
- Storage 持久化到磁盘。
- 完整自动重启退避策略。

---

## 四、如何运行示例插件

先构建示例插件：

```bash
go build -o plugins/example/example-plugin.exe ./cmd/example-plugin
```

### 插件目录解析规则

插件目录按以下优先级确定：

1. 环境变量 `VERIX_PLUGIN_DIR`（推荐，可指向绝对路径）。
2. 未设置时，默认使用 `verix.exe` 所在目录下的 `plugins/` 子目录。

因此有两种部署方式：

**方式 A：`verix.exe` 与 `plugins/` 放同一目录（无需 env）**

```text
D:\mcp\
├── verix.exe
└── plugins\
    └── example\
        ├── plugin.json
        └── example-plugin.exe
```

客户端只配置 `command: "D:\\mcp\\verix.exe"` 即可，无需 `env`。

**方式 B：显式指定插件目录（目录位置自由）**

```bash
VERIX_PLUGIN_DIR=D:\mcp\plugins verix.exe
```

MCP 客户端配置示例（Claude Desktop）：

```json
{
  "mcpServers": {
    "verix": {
      "command": "D:\\mcp\\verix.exe",
      "env": { "VERIX_PLUGIN_DIR": "D:\\mcp\\plugins" }
    }
  }
}
```

> 注意：开发时用 `go run .` 运行，`os.Executable()` 指向临时编译目录，默认插件目录不是项目目录。此时应显式设置 `VERIX_PLUGIN_DIR=plugins`（相对当前工作目录）。

启动主程序：

```bash
go run .
```

或者指定插件目录：

```bash
VERIX_PLUGIN_DIR=plugins go run .
```

然后通过 MCP 客户端执行：

```text
plugin_list
plugin_describe({"plugin":"com.verix.example"})
plugin_call({
  "plugin":"com.verix.example",
  "method":"echo",
  "arguments":{"message":"hello"}
})
```

Windows PowerShell 可以使用：

```powershell
$env:VERIX_PLUGIN_DIR = "plugins"
go run .
```

---

## 五、DAO 生成插件使用说明

`com.verix.dao` 是一个真实业务插件，根据 MySQL 表结构生成符合 Go GORM Model Standards 的 DAO 层代码。

### 构建

```bash
go build -o plugins/dao-plugin/dao-plugin.exe ./cmd/dao-plugin
```

### 方法

`list_tables`：列出当前数据库的所有表，先确认表名。

```json
{
  "plugin": "com.verix.dao",
  "method": "list_tables",
  "arguments": {
    "dsn": "root:pass@tcp(127.0.0.1:3306)/mydb?parseTime=true"
  }
}
```

`generate`：根据 DSN 和表名生成 GORM model 代码。

```json
{
  "plugin": "com.verix.dao",
  "method": "generate",
  "arguments": {
    "dsn": "root:pass@tcp(127.0.0.1:3306)/mydb?parseTime=true",
    "tables": ["user", "user_group"],
    "package_name": "model"
  }
}
```

返回 `files` 数组，每个元素包含 `table`、`entity`、`package` 和完整的 `code` 字符串。

### 生成的代码遵循的规范

依据 `.claude/skills/go-gorm-model-standards/SKILL.md`，生成内容包含：

- 实体 struct + `TableName()`。
- 排序字段常量和 allowlist。
- Query / Update / Delete 参数 struct（指针字段区分省略和零值）。
- Model wrapper + `NewXxxModel` 构造函数。
- `Create` / `GetById` / `ListByParam` / `UpdateByParam` / `DeleteByParam` / `BatchDelete` 及其 `WithTx` 事务变体。
- `Id` / `GroupId` 命名（非 `ID` / `GroupID`），snake_case 列名和 JSON tag。
- 参数化过滤、Count-before-pagination、allowlist 排序、`(nil, nil)` 未命中处理。

### 字段入选规则

| 生成位置 | 入选规则 | 生成形式 |
|---|---|---|
| `XxxQueryParams` 主键 | `COLUMN_KEY = PRI` | `Id` + `IdList`，单值优先于列表 |
| `XxxQueryParams` 字符串 | `varchar` / `char` / `enum` / `set` | 单值字段，查询时使用参数化 `LIKE` |
| `XxxQueryParams` 整形 | `bigint` / `int` / `integer` / `mediumint` / `smallint` / 非 `tinyint(1)` 的 `tinyint`，且非主键 | `Field` + `FieldList`，查询时单值等于优先，否则 `IN` |
| `XxxQueryParams` 时间 | `datetime` / `timestamp` / `date` / `time` | `FieldStart` + `FieldEnd`，使用 `>=` / `<=` |
| `XxxDeleteParams` 主键 | `COLUMN_KEY = PRI` | `*Id` + `IdList`，指针区分“未提供”和零值 |
| `XxxDeleteParams` 整形 | 与 QueryParams 整形规则相同，且非主键 | `*Field` + `FieldList`，删除时单值等于优先，否则 `IN` |
| `xxxOrderFieldMap` | 排除 text/blob/json/binary 等大字段，其它字段入选 | 排序常量 + allowlist + `clause.OrderByColumn` |

整形字段的单值优先规则，例如 `GroupId`：

```go
if params.GroupId != 0 {
    db = db.Where("group_id = ?", params.GroupId)
} else if len(params.GroupIdList) > 0 {
    db = db.Where("group_id IN ?", params.GroupIdList)
}
```

删除参数使用指针，因此可以表达显式零值：

```go
if params.GroupId != nil {
    db = db.Where("group_id = ?", *params.GroupId)
} else if len(params.GroupIdList) > 0 {
    db = db.Where("group_id IN ?", params.GroupIdList)
}
```

`tinyint(1)` 映射为 `bool`，不进入整形字段集合；普通 `tinyint` 映射为 `int8`，会进入。

生成代码会经过 `go/format` 格式化，保证输出语法正确、格式规范。

### 已知简化

- 实体注释当前使用表名（未读取 `information_schema.tables` 的表注释）。
- 主键默认按数值类型处理（`params.Id != 0`）；字符串主键会生成 `!= ""`。
- decimal 映射为 `float64`，如需精度可自行改为 `string`。
- 复数变量名简单加 `s`（`user` → `users`），不处理不规则复数。
