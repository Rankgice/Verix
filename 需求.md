🧠 数据库 MCP 实现规范（MVP版）
一、目标

实现一个 MCP Server，提供数据库访问能力，支持：

多数据库连接（MySQL 起步）
安全执行 SQL
提供 Schema 信息给 LLM
支持 AI 自动查询流程
二、技术选型（强制要求）
1. SQL 执行层
使用：database/sql
驱动：
MySQL：go-sql-driver/mysql
PostgreSQL（预留）：pgx

❌ 禁止使用：GORM（原因：无法控制 SQL、安全性差、不适合动态 SQL）

2. 项目结构（以当前项目结构为准）
三、核心架构设计
1. 多数据库抽象接口
type DBExecutor interface {
    Query(ctx context.Context, sql string, args ...any) (*QueryResult, error)
    Exec(ctx context.Context, sql string, args ...any) (*ExecResult, error)

    GetSchema(ctx context.Context) (*Schema, error)
    DescribeTable(ctx context.Context, table string) (*TableSchema, error)
}
2. 连接管理（必须实现）
type Connection struct {
    Name   string
    DB     *sql.DB
    Driver string
}

type Manager struct {
    conns map[string]*Connection
}

支持：

多连接（prod / test / readonly）
按 name 获取
3. SQL 执行流程（必须按此顺序）
execute_sql
   ↓
SQL 校验（禁止危险操作）
   ↓
SQL Rewrite（自动加 LIMIT）
   ↓
执行（database/sql）
   ↓
结果格式化（columns + rows）
四、MCP Tools 定义（必须实现）

共 5 个：

1️⃣ execute_sql
输入
{
  "connection": "string",
  "sql": "string",
  "params": {},
  "limit": 100,
  "timeout_ms": 2000,
  "readonly": true
}
输出
{
  "success": true,
  "data": {
    "columns": [
      {"name": "id", "type": "bigint"}
    ],
    "rows": [
      [1, "Alice"]
    ],
    "row_count": 1
  },
  "meta": {
    "latency_ms": 10,
    "truncated": false
  }
}
2️⃣ get_schema

👉 用于 AI 获取“全局表结构（轻量）”

输入
{
  "connection": "string"
}
输出
{
  "data": {
    "tables": [
      {
        "name": "users",
        "columns": ["id", "name", "email"]
      }
    ]
  }
}
3️⃣ describe_table

👉 用于 AI 获取“单表详细结构”

输入
{
  "connection": "string",
  "table": "string"
}
输出
{
  "data": {
    "table": "orders",
    "columns": [
      {
        "name": "id",
        "type": "bigint",
        "is_primary_key": true
      },
      {
        "name": "user_id",
        "type": "bigint",
        "is_foreign_key": true,
        "references": {
          "table": "users",
          "column": "id"
        }
      }
    ],
    "indexes": [
      {
        "name": "idx_user_id",
        "columns": ["user_id"]
      }
    ]
  }
}
4️⃣ list_tables
输入
{
  "connection": "string"
}
输出
{
  "data": {
    "tables": ["users", "orders"]
  }
}
5️⃣ analyze_query

👉 不执行 SQL，只分析风险

输入
{
  "sql": "string"
}
输出
{
  "data": {
    "operation": "SELECT",
    "tables": ["users"],
    "risk_level": "low",
    "warnings": ["No LIMIT clause"]
  }
}
五、安全要求（必须实现）
1. 禁止执行
DROP
TRUNCATE
DELETE（无 WHERE）
UPDATE（无 WHERE）
2. 自动增强
SELECT 必须加 LIMIT（默认 100）
超时控制（context）
3. readonly 模式
默认 true
非 SELECT 直接拒绝
六、Schema 实现（MySQL）

必须支持：

1. 获取表列表
SHOW TABLES;
2. 获取字段
SELECT 
  COLUMN_NAME, DATA_TYPE, IS_NULLABLE, COLUMN_KEY
FROM information_schema.columns
WHERE table_schema = DATABASE()
3. 索引
SHOW INDEX FROM table_name;
七、关键设计原则（必须遵守）
✅ 1. Tool 层不关心数据库类型
executor := GetExecutor(connection)
✅ 2. rows 使用二维数组
"rows": [
  [1, "Alice"]
]

❌ 不使用 map（节省 token + 提高性能）

✅ 3. schema 分层
接口	粒度
get_schema	全局（轻量）
describe_table	单表（详细）
✅ 4. 不做 ORM
不使用 GORM
不做 Model 映射
八、MVP 实现顺序（强制步骤）

按顺序实现：

✅ MySQL 连接 + database/sql
✅ execute_sql（只读 + LIMIT）
✅ list_tables
✅ get_schema
✅ describe_table
✅ analyze_query（简单版本）
九、非必须（后续扩展）
PostgreSQL 支持
SQL AST 解析
权限系统
审计日志
NL → SQL
十、一句话总结

👉 这是一个：

“安全的、AI 可调用的数据库查询网关（非 ORM）”